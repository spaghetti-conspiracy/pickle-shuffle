[project]
name = "pickle-shuffle"
version = "0.4.0"
description = "ピックルボール練習会の対戦カード生成アプリ"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "sqlalchemy>=2.0",
    "pydantic>=2.6",
    "segno>=1.6",
    # **本番（Vercel + Neon）では必須。** extra にしていた頃、Vercel が
    # `uv.lock` を見るようになった時点で psycopg が入らなくなり、
    # `ModuleNotFoundError: No module named 'psycopg'` で全リクエストが 500 になった。
    # 手元は SQLite で動くが、入っていても害は無いので通常の依存に置く。
    "psycopg[binary]>=3.1",
]

[project.optional-dependencies]
# 後方互換のために残す（`pip install -e ".[postgres]"` と書いた手順書がある）。
# 中身は通常の依存に移したので空でよい。
postgres = []

# 開発用は extras ではなく依存グループに置く。extras だと `uv sync` が
# 既定で入れないので、CLAUDE.md の手順どおりに進めてもテストが動かない。
[dependency-groups]
dev = [
    "pytest>=8.0",
    "httpx2>=2.13",
    "ruff>=0.4",
    # ブラウザテスト用。**dev に入れておく。** 入っていないと
    # `pytest -m browser` が「0件実行で緑」になり、release の前提が空振りする。
    "playwright>=1.44",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]

[tool.setuptools.package-data]
app = ["static/*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# リポジトリのルートを import できるようにする。テスト同士が
# `from tests.test_api import ...` で助け合っているため。
# `python -m pytest` では CWD が入るので気づけない（`pytest` では入らない）。
pythonpath = ["."]
# ブラウザテストは既定で外す。Playwright を入れていない手元でも、
# ユニットテストはそのまま通る。走らせるときは `pytest -m browser`。
addopts = "-q -m 'not browser'"
markers = ["browser: 実際のブラウザで画面を操作する（Playwright が要る）"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
