# PLAN.md — 開発手順と進捗

仕様: `doc/spec.md` / 開発規則: `CLAUDE.md`

このファイルは進捗の引き継ぎを兼ねる。フェーズを完了したらチェックを入れ、
判断を変えた点は「決定の変更履歴」に追記すること。

---

## 1. 何を作るか

ピックルボール練習会（16名程度）の対戦カードを自動生成するウェブアプリ。

練習会中は、遅刻・中座・休憩でメンバー集合が刻々と変わる。その中で
「参加回数が公平 / ペアと対戦がばらける / 初心者への配慮 / なるべく男女ミックス」を
同時に満たすカードを手作業で組むのは難しい、という問題を解く。

個人 PC 上で `docker compose up` するだけで起動し、タブレットのブラウザから
試合表示画面を常時表示して使う。記録は練習会終了後に破棄する前提。

## 1.1 利用状況（規模の前提）

- 1試合 7分、1時間あたり 8試合程度、練習試合の時間は最大 3時間程度
- したがって **1練習会あたりのラウンド数は最大 25 程度**、メンバーは 16名程度
- 1練習会の `round_participation` は最大でも 25 x 16 = 400行程度。
  設計の要 A（スナップショットからの毎回導出）のコストは無視できることがこれで裏づけられる
- テストのラウンド数は、この実運用レンジ（20〜30ラウンド）に合わせて選ぶ
- 参考: 13名・24ラウンドだと1人あたり約15試合。12人の中から14回ペアを組むので
  ペアの重複は必ず起きる。「重複をゼロにする」ではなく「重複回数を均す」が正しい目標になる

## 2. 確定済みの決定事項

| 項目 | 決定 |
|---|---|
| 技術スタック | Python 3.11 + FastAPI + SQLAlchemy 2.0 + SQLite、フロントは素の HTML/CSS/JS（ビルド不要） |
| DB | PostgreSQL。ローカルは docker compose が DB コンテナごと立ち上げ、Vercel は Neon を使う。`DATABASE_URL` で切替でき SQLite でも動く（テストはインメモリ SQLite） |
| デプロイ先 | Vercel（サーバーレス関数 + Neon Postgres）。手元の PC では docker compose |
| ボタン挙動 | 「開始」= 採用（参加回数に反映） / 「スキップ」= 不採用のまま再生成 / 試合後「次のマッチ」で次を生成 |
| 性別 | `male` / `female` / `other`。`other` はミックス扱いで減点しない |
| レベル | 3種を保存（`beginner` / `racket_experienced` / `pickleball`）。生成ロジック上は後者2つを同一視 |
| メンバー並び順 | 手動並べ替えは実装しない |
| 練習会 | 複数作成・切替可。記録破棄用の削除ボタンあり |
| コート数 | 練習会の作成時に最大数を指定（既定2）。途中で個々のコートを試合用から外す/戻すことができる |
| コートを外す理由 | 初心者の育成用の練習コートにするため。コーチングする経験者も試合から外れる。外れるメンバーは休憩扱い |
| クライアント構成 | 管理画面と表示画面は別端末から同時アクセスする前提 |
| メンバー変更の反映 | 登録画面で確定した時点で DB に反映。マッチへの反映は次の生成時。表示中のカードは自動で変えない |
| 乱数シード | 練習会の作成時に固定シードを採番して保存。以降の非決定性はすべてこのシードから導出 |
| メンバー属性の再利用 | ニックネームをキーに練習会をまたいで属性（性別・レベル）を引き当てる。**統計は共有しない**。重複時は last-write-wins（振動してよい） |

---

## 3. 設計の要

実装上の判断の根拠。ここを変える場合は必ず理由を「決定の変更履歴」に残すこと。

### A. 統計は「累積カラム」ではなく「スナップショットからの導出」

採用ラウンドごとに、その時点の全メンバーの状態を `round_participation` に1行ずつ記録する
（`played` / `sat_out` / `resting`）。参加回数・休憩クレジット・連続不参加は毎回ここから計算する。

- 二重計上や更新漏れが構造的に起きない
- 「間違って開始を押した」の取り消しが、ラウンド行の削除だけで完結する
- 不採用ラウンドはスナップショットを持たないので、統計に一切影響しない（仕様要件）
- コストは O(ラウンド数 x メンバー数) で無視できる

### B. 公平性はハードな枠で担保する（既定は厳密公平）

出場者候補を `adjusted <= (N番目に小さい adjusted)` に限定する。これで参加回数の差は
必ず 1 以内に収まる。`fairness_slack` を 1 以上にすれば枠を緩められるが、既定は 0。

当初は「候補集合が枯れたときだけ枠を広げる適応ルール」を入れる計画だった。
16名8枠を厳密公平で回すと出場者が1ラウンドおきの交代に固定され、
ペアがばらけなくなる、という想定だったため。**実測の結果この想定は誤りだった**:

| | 参加回数差 | ペア重複の最大 | 相異なるペア数 |
|---|---|---|---|
| 枠を広げる | 2 | 2 | 74.6 |
| 厳密公平 | **0** | 2 | 75.8 |

出場者が交代制になっても、コート内のペアの組み方は自由に選べるのでばらけ方は落ちない。
交代そのものは、16名8枠で参加回数を揃えるなら数学的に必然であり、避けるべきものでもない。
適応ルールは公平性を損なうだけだったので取りやめ、厳密公平を既定にした。

### C. スコアは分解でき、近似なしの全探索が可能

ラウンド後の参加回数の分散最小化は、ラウンドで増える総プレー数が固定なので
`Σ(adjusted_after)^2` の最小化と等価であり、さらに選手ごとの線形項
`Σ_{出場者} (2*adjusted + 1)` に分解できる。

これによりスコア全体が「選手項 + ペア項 + 試合項 + ラウンド項」に分解でき、

- ペア項を先に 28 通り（8人の全ペア）precompute
- 試合項（ペア x ペア）を 210 通り precompute
- 編成は 8人なら 315 通り、4人なら 3 通りを **全列挙**

候補集合を最大 60 個試しても 100ms 未満で **そのラウンドの厳密な最小解** が得られる。

### C-2. 1ラウンドだけを見る貪欲法から、先読みへ

1ラウンドの最小解を積み重ねても、練習会全体では最小にならない。今このペアを使うと
その先で組める相手が減る、という近視眼が起きるため。そこで上位 `beam` 件の候補について
その先 `lookahead` ラウンドを貪欲に進めてみて、累積コストで選び直す。
候補ごとの先読みには同じ乱数種を使い、同じ運の下で比べる（共通乱数法）。

生成はボタン1回につき1度で、試合間隔は7分ある。速度より品質を優先してよい。

実測（9構成 x 8シードの対応比較、重複回数の二乗和の理論超過）:

| | 平均超過 | 所要 |
|---|---|---|
| 貪欲 (lookahead=0) | 16.9% | 25ms |
| 先読み1 (beam=16) | **15.3%** | 100ms |

9構成すべてで改善した。深さ2以上、beam 48 以上にしても改善しない
（先の手の読みが粗いため）。候補集合の上限を増やすとかえって悪化する
（今のラウンドだけを見て選ぶほど近視眼が強まる）。

### C-3. 品質はどう測ったか

重みを変えると「重み付き合計コスト」も変わるので、それでは配分どうしを比べられない。
そこで **重複回数の二乗和が、同じ総数を可能なペアすべてに均等配分したときの
理論最小値からどれだけ超過しているか** を指標にした。重みに依存しないので公平に比べられる。

この指標で決めた主な調整:

- **対戦相手の重み 30 → 60**: 合計超過 19.9% → 14.4%。1ラウンドで作られるペアは4組、
  対戦は8組なので、この比なら両者の影響が釣り合う（1件あたりはペアが2倍）
- **男女構成の作り直し**: 「男子ペア対女子ペア」だけを別枠の準ハード制約に分離し、
  残りの減点はペア重複1回ぶんより小さくした。以前は男女構成（優先度6）が
  ばらけ（優先度1）を押しのけており、仕様の優先順位と逆転していた
- **初心者と組む回数の重み 40 → 120**: 偏りが 3 → 2 に減り、ばらけへの影響はほぼ無し
- **初心者ペアの集中 400 → 700**: 同一コートへの集中が 86% → 96%

最終的な実測（5〜16名の12構成、3時間ぶん24〜25ラウンド）:

- 参加回数差 0〜1 / 連続してマッチに入れない回数 2以下
- 初心者同士ペア 0件 / 初心者ペアの同一コート集中 90〜94%
- 男子ペア対女子ペア 0件
- **全員が他メンバーのほぼ全員と同じコートに入る**（16名24ラウンドで、1人あたり15人中14〜15人。全員必達は回り方次第で保証できない）
- 1生成 最大 160ms

### D. 「メンバーの並びが透けない」ための非決定性設計

類似ツールにありがちな「登録順・ID順がマッチに透けて見える」「同じ人数だと毎回同じ展開になる」
を構造的に潰す。**再現性は保ちつつ、練習会ごとに違う挙動にする。**

- **D-1. 練習会シード** — `practice_sessions.random_seed`(64bit) を作成時に `secrets` で採番。
  各生成の RNG は `Random(hash((seed, round_seq, attempt)))` として決定的に導出する。
  `attempt` はその `round_seq` で不採用になった回数で、スキップのたびに必ず別の抽選になる。
- **D-2. 順序正規化** — アルゴリズムの入口で `tie_key(seed, member_id)` によりソートし直し、
  以降はこの正規化順のみを使う。入力順が結果に影響せず、かつ走査順が登録順に見えない。
- **D-3. 同点は一様抽選** — スコア最小が複数あるとき先頭を採らない。reservoir sampling で
  同点集合から一様に選ぶ。浮動小数の微小ジッタは使わず、スコアは整数で保持する。
- **D-4. 表示レベルのランダム化** — コート割当・チーム番号・ペア内の並びもシャッフルする。

---

## 4. 実装フェーズ

### Phase 0: ドキュメント定義

- [x] `CLAUDE.md`（開発規則）
- [x] `PLAN.md`（本ファイル）
- [x] ユーザーレビューと合意

**DoD**: ユーザーが両ファイルをレビューし、合意が取れること。

---

### Phase 1: プロジェクト雛形とコンテナ

- [x] `pyproject.toml` — 依存: fastapi, uvicorn[standard], sqlalchemy>=2, pydantic>=2 /
      dev: pytest, httpx, ruff / optional `postgres`: psycopg[binary]
- [x] `Dockerfile` — python:3.11-slim、非 root ユーザ、`pip install ".[postgres]"`
- [x] `docker-compose.yml` — web + PostgreSQL。Vercel と同じ DB にして
      「ローカルでは動くのに本番で壊れる」を防ぐ
- [x] `app/config.py` — `Settings`（`DATABASE_URL`, `PORT`, スコア重み一式,
      `fairness_slack`=0, `lookahead`=1, `beam`=16, `max_candidate_sets`=60）
- [x] `app/db.py` — engine / sessionmaker / `Base` / `get_db`。SQLite は `check_same_thread=False`
- [x] `app/main.py` — 起動時 `create_all`、`/api` ルータ、`StaticFiles` で `app/static` を配信

**DoD**: `docker compose up` 後 `GET /api/health` が 200。

---

### Phase 2: データモデル（`app/models.py`）

- [x] `practice_sessions` — id, name, created_at, rotation(0/90/180/270),
      **random_seed（作成時に採番・不変）**
- [x] `courts` — id, session_id, court_index(0起点), name, in_use(bool)。
      作成時に最大数ぶん作る。途中で `in_use` を切り替えて試合用から外す/戻す
- [x] `members` — id, session_id, nickname, gender, level, status(active/resting/left),
      baseline, created_at
- [x] `rounds` — id, session_id, seq（採用時のみ採番, nullable）,
      status(pending/adopted/rejected), attempt, created_at, decided_at
- [x] `matches` — id, round_id, court_id
- [x] `match_slots` — id, match_id, team_index(0/1), member_id
- [x] `round_participation` — id, round_id, member_id, state(played/sat_out/resting)
      **採用ラウンドのみ**
- [x] `people`（Phase 11 で `member_profiles` から改称） — id, owner_id, nickname（**一意にしない**）,
  gender, level, external_id（`bear:9001`）, created_at, updated_at
      練習会に属さないグローバルなテーブル

補足:

- `baseline` は途中参加者が過度に優先されないよう、**参加登録時点の active メンバーの最小 `adjusted`**
  を格納する（active が居なければ 0）。これ以外にメンバー行へ統計を持たせない。
- メンバー削除は原則ソフト削除（`status=left`）。どのラウンドにも登場していない場合のみ物理削除可。

#### メンバー属性の練習会をまたいだ再利用

- `people` は **属性（性別・レベル）だけを保持する台帳** であり、練習会には属さない。
- **統計（参加回数・ペア履歴・休憩）は絶対に練習会をまたがない。** 共有するのは属性のみ。
  `members.session_id` が統計のスコープを決めるという構造を崩さないこと。
- メンバーの登録・更新時に `nickname` をキーに upsert する。**last-write-wins で振動してよい**
  （入力項目が少ないため、厳密なマージやコンフリクト解決は行わない）。
- 練習会を削除しても `people` は残る。台帳自体の手入れはメンバー管理画面で行う（Phase 11）。
- **同一練習会内での同名は許可する**（禁止しない）。運用上はニックネームを付け替えて解決する想定なので、
  アプリ側は **警告を出すだけ** にとどめる。
- 生成アルゴリズムは常に `member.id` を扱うので、同名が存在しても破綻しない。

**DoD**: `tests/test_models.py` でテーブル作成・基本 CRUD・カスケード削除が通る。
`people` が練習会削除の影響を受けないこと。

---

### Phase 3: 生成アルゴリズム（最重要・DB 非依存）

- [x] `app/scheduler/domain.py` — `Gender`, `Level`, `MemberStatus`, `PlayerStat`, `History`,
      `Pair`, `Match`, `RoundPlan`
- [x] `app/scheduler/stats_rules.py` — スナップショット列から
      `plays` / `rest_credit` / `sit_out_streak` / `just_returned` を導出する純粋関数
- [x] `app/scheduler/generator.py` — `generate_round(...) -> RoundPlan`

#### 導出規則（`stats_rules.py`）

- `plays` = `played` の数
- `rest_credit` = `resting` の **連続ブロック** それぞれについて `長さ - 1` の総和
  （仕様「2試合分以上まとめて休んでも1試合分の不参加扱い」の実装）
- `sit_out_streak` = 末尾から続く `sat_out` の数
- `just_returned` = 現在 active かつ直近スナップショットが `resting`
- `adjusted` = `baseline + plays + rest_credit`

#### 生成手順（`generator.py`）

0. **順序正規化** — 入力を `tie_key(seed, member_id)` でソートし直す（設計の要 D-2）
1. **出場人数の決定** — active 8人以上 → 2面8名 / 4〜7人 → 1面4名 / 4人未満 → `NotEnoughPlayers`
   - 使用するコートは **`court_index` の小さい方から詰める**。会場での迷いを避けるため、
     ここだけは D-4 のランダム化を適用しない（どの編成をどのコートに置くかのシャッフルは
     2面とも使う場合にのみ行う）
   - `RoundPlan` は `n_courts`（練習会のコート数）と実際に使う `matches` の両方を持ち、
     **使わないコートがあることを表示側が判別できる** ようにする
2. **候補集合の列挙** — `adjusted` 昇順。N番目の値を閾値 T とし、`adjusted < T` を必須、
   `T <= adjusted <= T + slack` を柔軟枠として組合せを列挙。slack は設計の要 B の適応ルールで決定。
   組合せ数が `max_candidate_sets` を超えたら注入 RNG で重複なくサンプリング
3. **編成の全列挙** — 候補集合ごとにペア分割 x コート割当を全列挙（8人=315、4人=3）
4. **スコア最小を採用** — 整数スコア。同点集合から reservoir sampling で一様抽選（D-3）
5. **表示レベルのランダム化** — コート割当・チーム番号・ペア内の並びをシャッフル（D-4）

#### スコア項目（低いほど良い。重みは `config.py` で調整可能）

| 仕様優先度 | 項目 | 実装（増分形は二乗和最小化＝均等化と等価） |
|---|---|---|
| 1 | ペア重複 | `Σ_{4ペア} w_partner * (2*partner_count + 1)` |
| 1 | 対戦相手重複 | `Σ_{8組の対戦} w_opponent * (2*opponent_count + 1)` |
| 2 | 参加回数の公平性 | `Σ_{出場者} w_fair * (2*adjusted + 1)` ＋ 候補集合の slack 制限（ハード） |
| 3 | 連続不参加 | `Σ_{非出場のactive} w_sitout * (sit_out_streak + 1)^2` |
| 3 | 初心者同士ペア | 両方 beginner なら `w_beginner_pair`（実質ハード制約の大きな値） |
| 4 | 初心者の相手の偏り | `w_bspread * (2*beginner_partner_count + 1)` |
| 5 | 初心者ペアの集中 | `w_bconc * (初心者を含むペアが片側だけの試合の数)` |
| 6 | 男女組合せ | 試合ごとに `w_gender *` {MXvMX:0, MMvMM/FFvFF:1, MXvMM/MXvFF:2, MMvFF:6} |
| 7 | 休み明け優先 | 非出場の `just_returned` 1人につき `w_return`（小さめ） |
| — | 再生成の差別化 | `avoid` と一致で大きな減点、1コート一致で小さめの減点 |

ペア種別: 両方 male=MM / 両方 female=FF / それ以外（`other` を含む）=MX。

初心者ペアの集中について: 「片側だけに初心者ペアがある試合の数」を直接数えることで、
「初心者2名を同じ試合の対面に配置する」が「初心者1名だけ出す」より良いと評価される
（前者は該当0試合、後者は1試合）。結果として初心者は偶数で同時に出す方向へ自然に誘導される。

#### `tests/test_generator.py` が検証する性質

**公平性**

- [x] 人数 8/9/10/12/16 それぞれ 30ラウンドで、参加回数の最大差が `fairness_slack_max + 1` 以内
- [x] 8人8枠では全員が毎ラウンド出場し、参加回数差が常に 0
- [x] 途中参加者が baseline により連続出場で過度に優遇されない
- [x] 休憩3ラウンド連続後の復帰時、`adjusted` の欠損がちょうど 1（休憩クレジット）
- [x] 連続不参加が 2 ラウンド続く事象が、9〜11人の設定でほぼ発生しない

**ばらけ（優先度1）**

- [x] 16人8枠を 20ラウンド回したとき、出場者集合が「前回の裏返し」に固定されない
- [x] 同じペアが 2 回目に現れる前に、より多くの異なるペアが出現している

**並び順の非依存性（設計の要 D）**

- [x] 入力リストの順序をシャッフルしても、同一シードなら結果が完全に一致する
- [x] member_id を振り直しても出場者の選ばれ方に系統的な偏りが出ない
- [x] 履歴ゼロの初回生成を多数のシードで実行したとき、特定のペア・コート割当に偏らない
- [x] シードが異なれば結果が異なる
- [x] 同一シード・同一入力・同一 attempt で結果が完全に再現する
- [x] `avoid` を与えると別の編成が返る

**初心者（優先度3〜5）**

- [x] 初心者2名以上でも beginner x beginner ペアが 0 件（初心者が過半数の異常系を除く）
- [x] 初心者が2名同時に出場するとき、両者のペアが同一コートで対戦する
- [x] 初心者と組む回数が非初心者間で均されている

**男女（優先度6）**

- [x] 男女均等16名: MXvMX が支配的、MMvFF が 0 に近い
- [x] 男性12/女性4: MMvMM と FFvFF が調整として現れ、MMvFF は避けられる
- [x] `other` を含むペアがミックス扱いで減点されない

**境界・縮退**

- [x] 人数 4/5/6/7/8/9/13/16 の各ケースで妥当な編成、3人以下で `NotEnoughPlayers`
- [x] 全員同性・全員初心者・全員休憩などの縮退ケースでクラッシュしない
- [x] 重複出場者がいない、出場者数はちょうど `使用コート数 * 4` 名（不変条件）

**コートが埋まらないケース（8名未満）**

- [x] **4〜7名では使用コートが1面だけになり、出場者は4名ちょうど**
- [x] **使われないコートが `RoundPlan` から判別できる**（`n_courts=2` かつ `len(matches)==1`、
      使用コートは `court_index=0`）
- [x] 5名/6名/7名では余った 1〜3 名が `sat_out` になり、次ラウンドで優先的に出場する
      （少人数ほど連続不参加が目立つため、`sit_out_streak` が確実に解消されること）
- [x] 7名で 14 ラウンド回して参加回数の最大差が 1 以内（4/7 ずつ回るので厳しい条件）
- [x] 途中で 7名 → 8名に増えたら、次の生成から 2面とも使われる
- [x] 途中で 8名 → 7名に減ったら、次の生成から 1面だけになる

**13名のケース（もっともありがちな規模）**

- [x] 13名8枠で 26 ラウンド回して参加回数の最大差が `fairness_slack_max + 1` 以内
- [x] 同じく連続不参加が 2 ラウンド続く事象がほぼ発生しない（毎ラウンド5名が休むため）
- [x] 13名（男8/女5・初心者1名）という現実的な構成で、
      初心者同士ペアが 0 件、MMvFF が 0 に近い、ペア重複が均されている

- [x] `tests/test_stats_rules.py` — 休憩ブロック・連続不参加・復帰判定をスナップショット列で検証

**DoD**: 上記すべてのテストがパスすること。

---

### Phase 4: 永続化サービス層（`app/services/`）

- [x] `stats.py` — DB → `PlayerStat` / `History` の組み立て（採用ラウンドのみ走査し `stats_rules` で導出）
- [x] `rounds.py` — 生成 / 採用 / 不採用 / 取り消しの遷移
  - 生成: 既存 pending があれば rejected にしてから新規 pending を作る。
    不採用になった編成の署名を `avoid` として渡す
  - 採用: 1トランザクションで `seq` 採番 + `round_participation` を全非 left メンバー分書き込む
  - 不採用: status を rejected にするだけ。`attempt` が進む
  - 取り消し: **最新の採用ラウンドのみ** 削除可（スナップショットも消え統計が自動的に戻る）

#### メンバー変更の反映タイミング

- メンバーの追加・休憩・復帰・削除・属性変更は、登録画面で確定した時点で DB に反映される
- ただし **表示中のカードは自動で書き換えない**。マッチへの反映は **次の生成が走ったとき**
  （「スキップ」または「次のマッチ」）。試合中に画面が勝手に変わらないようにするため
- 生成は常に「生成した瞬間のメンバー状態」を読む。pending を後から無効化する処理は作らない
- 表示画面は、pending に休憩中/削除済みのメンバーが含まれる場合のみ小さな注意表示を出す。操作は強制しない
- メンバーの物理削除は、どのラウンド（pending 含む）にも登場していない場合のみ許可

**DoD**: `tests/test_rounds_service.py` — 採用で統計が反映される / 不採用は一切影響しない /
取り消しで完全に元に戻る / メンバーを変更しても pending は変化せず次の生成で初めて反映される /
不採用のたびに `attempt` が進み別の編成が生成される。

---

### Phase 5: API 層（`app/api.py` + `app/schemas.py`）

```
GET    /api/health
GET    /api/sessions                        練習会一覧
POST   /api/sessions                        作成 {name}
GET    /api/sessions/{id}                   詳細（コート名・回転含む）
PATCH  /api/sessions/{id}                   name / court_names / rotation の更新
DELETE /api/sessions/{id}                   記録破棄（カスケード削除）
GET    /api/sessions/{id}/members
POST   /api/sessions/{id}/members           {nickname, gender, level}
PATCH  /api/members/{id}                    {nickname,gender,level,status} 休憩/復帰もここ
DELETE /api/members/{id}                    ソフト削除（履歴が無ければ物理削除）
GET    /api/sessions/{id}/current           表示画面用
POST   /api/sessions/{id}/rounds/generate   生成（pending を作る）
POST   /api/rounds/{id}/adopt               採用（=「開始」）
POST   /api/rounds/{id}/reject              不採用（=「スキップ」の前半）
POST   /api/rounds/{id}/undo                最新の採用ラウンドの取り消し
GET    /api/sessions/{id}/stats             参加回数・ペア履歴（管理画面の確認用）
GET    /api/member-profiles                 ニックネーム辞書の一覧（登録画面の補完用）
GET    /api/member-profiles/{nickname}      1件取得（無ければ 404）
DELETE /api/member-profiles/{nickname}      辞書から削除
```

`/current` が返すもの:

- 現在のラウンド（pending または最新の adopted）とその編成
- メンバー一覧と状態
- `revision` — **ラウンドの状態変化にのみ連動する** 単調増加値。表示画面はこれが変わったときだけ
  再描画する。メンバーを編集しただけでは変わらず、表示中のカードは動かない
- `member_changed_since_generated` — pending 生成後にメンバーが変更されたか（注意表示用）
- `duplicate_nicknames` — 現在のラウンドに同名が2名以上含まれる場合、その一覧（警告表示用）

**DoD**: `tests/test_api.py` —

- [x] 「練習会作成 → 16名登録 → 生成 → 採用 → 再生成 → 休憩 → 生成」の一連フローが通る
- [x] メンバーを変更しても `/current` の編成と `revision` が変わらない
- [x] その後に生成を走らせると初めて変更が反映される
- [x] 練習会を2つ作ると `random_seed` が異なる
- [x] メンバー登録時に `people` に行ができ、台帳から選べば属性が引き当てられる（Phase 11 で改称）
- [x] **別の練習会で同じニックネームを登録しても、参加回数などの統計は引き継がれない**（重要）
- [x] 同名を同じ練習会に2名登録でき、警告フラグが立つ
- [x] 同一ラウンドに同名が含まれると `duplicate_nicknames` に出る
- [x] 練習会を削除しても `people` は残る（Phase 11 で改称）

---

### Phase 6: 管理画面と全体表示画面（`app/static/`）

画面は3つに分ける。役割と利用者が違うため。

| 画面 | 誰が | 何に使う | 端末 |
|---|---|---|---|
| 管理画面 `index.html` | 管理者 | メンバー登録・状態変更、コートの増減 | タブレット |
| 全体表示画面 `overview.html` | 練習会のリーダー | 全コートの現在のマッチを読み上げる。開始・スキップを決める | タブレット |
| メンバー用画面 `member.html` | 参加メンバー | 自分のコートの状況を見る（Phase 7） | スマートフォン |

**管理画面**

- [x] 練習会の作成 / 選択 / 削除。コート数は作成時に 1〜4 で指定
- [x] メンバー登録テーブル（ニックネーム・性別・レベル）、各行に「休憩 / 復帰」「削除」
- [x] ニックネーム入力欄に `<datalist>` で過去の登録名を補完。既存の名前を入れると
      性別とレベルが埋まる
- [x] 同一練習会内に同名がいる行に警告（登録自体はブロックしない）
- [x] コートごとに「試合で使う / 練習コート」を切り替え、名前も変更できる
- [x] 参加回数の一覧（公平性の目視確認用）
- [x] 全体表示画面へのリンク

**全体表示画面**

- [x] コートを縦に並べて一覧。スクロール可。4面でも1画面に収まる
- [x] 各コートにコート名と4名を「ペア vs ペア」で表示
- [x] 使わないコートは「練習コート」と「人数が足りません」を書き分ける
- [x] 待機中・休憩中のメンバーを脇に一覧表示（リーダーが読み上げるため）
- [x] 「開始」「スキップ」「次のマッチ」。下端に固定し、誤タップを避けて離す
- [x] **QR コード**を表示。読み取るとメンバー用画面が開く
      （`GET /api/sessions/{id}/member-qr.svg`。URL はリクエストのホストから組み立てるので
      手元の LAN IP でも Vercel でも正しい）
- [x] 2秒間隔のポーリングで `revision` を比較し、ラウンドが変わったときだけ再描画
- [x] 他端末で既に採用済みなら 409 を受けてポーリングで追従
- [x] 同名が2名含まれる場合に控えめな警告
- [x] **回転機能は廃止**。スクロール可・縦積みにしたので不要になり、コート名で会場と
      対応させる方針にした。DB・API・画面から完全に削除した

**DoD**: ブラウザ（Playwright + Chrome）で17項目を確認済み。
2面・3面・4面のスクリーンショットを目視してレイアウト崩れが無いことも確認した。

---

### Phase 7: メンバー用画面（`app/static/member.html`）

- [x] コートをタブで切り替える。タブにはコート名を出す
- [x] 選んだコートの現在のマッチを大きく表示。スマートフォンの縦画面前提
- [x] **読み取り専用**。開始・スキップは置かない（決めるのはリーダー）
- [x] 2秒ごとのポーリングで自動同期。全体画面で次のマッチに進めば自動で追従する
- [x] 選んだコートを `localStorage` に覚える
- [x] 練習コート・人数不足のコートもタブに出し、状態を表示する
- [x] 待機中・休憩中を下端に表示

**DoD**: ブラウザで11項目を確認済み。全体表示画面（タブレット相当）と
メンバー用画面（スマートフォン相当）を同時に開き、リーダーの生成・開始が
自動で伝わることを確認した。

---

### Phase 8: サブエージェントによるコードレビュー（実装完了後にまとめて実施）

**進め方（ユーザーの指示）**: Phase 8 まで実装を終えてから、Phase ごとにレビューし、
レビューに基づく修正を入れた状態を PR にする。PR の取り込みはユーザーが行う。

ブランチ運用（ユーザーの指示）: `main` の書き換えは禁止。それ以外のブランチは
`--force-with-lease` を使ってよい（`--force` は禁止）。
レビュー修正は該当する Phase のブランチに入れ、後続のブランチを rebase して積み直し、
`--force-with-lease` で push する。ブランチ名は `phase-*` のまま。

レビューは Phase ごとに、独立したサブエージェントに担当させる。
各エージェントには `doc/spec.md`・`doc/algorithm.md`・`CLAUDE.md` を必ず読ませる。

- [x] **Phase 1-2（雛形・データモデル）** — 設定の集約、DB 固有機能への依存、
      カスケード削除とソフト削除の整合、Enum の可搬性、サーバーレスでの接続の扱い
- [x] **Phase 3（生成アルゴリズム）** — 仕様の優先度1〜7が正しく反映されているか、
      重みの大小関係が優先順位と矛盾しないか、候補集合の列挙に漏れや偏りが無いか、
      境界条件（4/5/7/8/9人、初心者が過半数、全員同性、全員休憩）で破綻しないか。
      **非決定性の監査**: 登録順・ID順が出力に漏れる経路（`sorted` のタイブレーク、
      `set`/`dict` の反復順、`combinations` の列挙順、同点の先頭採用）が残っていないか、
      シード導出に時刻や `secrets` が混入していないか
- [x] **Phase 4-5（サービス層・API）** — トランザクション境界、統計の導出規則の正しさ、
      `rest_credit` / `baseline` の計算漏れ、`app/scheduler/` が DB 層を import していないこと、
      API の入力検証、コートの増減まわりの整合、別端末からの同時操作
- [x] **Phase 6（フロントエンド）** — タブレットでの表示崩れ、ポーリングの競合と取りこぼし、
      試合中に表示が勝手に変わらないこと、未使用コートと練習コートの区別
- [x] **テスト健全性の監査**（全 Phase 横断）— `CLAUDE.md` の
      「テストを弱体化させない規則」に照らして、緩いアサーション、skip/xfail、
      失敗を握りつぶす try/except、実装出力をコピペした固定値照合、
      検証対象自身のモック、トートロジー的アサーションが無いかを点検する。
      **「テストが全部通っている」ことではなく「テストが本当に仕様違反を検出できるか」を見る。**
      疑わしい箇所は実装にわざと欠陥を入れて当該テストが落ちるかを確認して報告する

- [x] 指摘を集約し、対応要否を判断して修正 → `pytest` / `ruff` 再実行
- [x] 各 Phase ブランチを積み直して push し、PR を作成

**実施済み（2026-09-23）。** チェックは付け忘れていたので、2026-09-25 に記録から確かめて付けた。
各 Phase の指摘への対応は、同日のコミット `fix(phase1): 手順どおりに開発環境が作れるようにする`（40da8fa）、
`fix(phase2): 消えると困る重複を DB で禁じ、テストが本番 DB を触らないようにする`（82fe1d2）、
`fix(phase3): 初心者同士ペアを作ってしまう探索の欠陥を直す`（d93cc7a）、
`fix(phase4-5): 別端末の同時操作でラウンドが壊れないようにする`（1657403）と
`fix(phase4-5): revision を応答の中身から導き、入力の長さを検証する`（f18d03b）、
`fix(phase6): 表示画面が無言で失敗したり、勝手に巻き戻ったりしないようにする`（b5cc71e）ほか。
指摘とそれに基づく判断（テストの監査で見つかった事後記録を含む）は `docs: レビューで見つかった不具合と、
その対応を記録する`（73496fa）で「決定の変更履歴」に残した。テストの監査で「実装を壊すと落ちるか」を
確かめた跡は d93cc7a のテスト側にある（`beginner_spread` を切った対照テストと、閾値を `<= 2` から
`<= 1` に強めた変更）。PR は #2〜#6 と、Phase 1 を main に入れた #11（いずれも取り込み済み）。
上のハッシュは今の main 上のもので、main の履歴を作り直したため PR のページのコミットとは違う。
その後は PR ごとに設計レビューとコードレビューを行う（`CLAUDE.md` の「作業の進め方」。PR #23）。

### Phase 9: 仕上げ

- [x] `README.md` — 起動手順、タブレットからのアクセス、DB 差し替え、記録破棄、**Vercel デプロイ手順**
- [x] `.gitignore` / `.dockerignore` / `.vercelignore`
- [x] **Vercel 対応** — `api/index.py`（ASGI エントリ）、`vercel.json`、`requirements.txt`。
      サーバーレスでも**接続は使い回す**（定常1本＋あふれ分だけ）。テーブル作成は同時起動と
      競合しても実際に揃っていれば続行する。`python -m app.init_db` で明示的に作ることもできる
- [x] `doc/algorithm.md` — 最終的なアルゴリズムの説明

**DoD**: クリーンな状態から `docker compose up --build` で起動し、README の手順どおり一連の操作が完了する。

---

## 5. 想定ディレクトリ構成

```
pickle_shuffle/
├── CLAUDE.md
├── PLAN.md
├── README.md
├── pyproject.toml
├── Dockerfile
├── docker-compose.yml
├── doc/spec.md
├── app/
│   ├── main.py
│   ├── config.py
│   ├── db.py
│   ├── models.py
│   ├── schemas.py
│   ├── api.py
│   ├── errors.py
│   ├── scheduler/          # DB・Web 非依存
│   │   ├── domain.py
│   │   ├── stats_rules.py
│   │   └── generator.py
│   ├── services/
│   │   ├── stats.py
│   │   └── rounds.py
│   └── static/
│       ├── index.html / admin.js
│       ├── display.html / display.js
│       └── style.css
└── tests/
    ├── conftest.py
    ├── test_stats_rules.py
    ├── test_generator.py
    ├── test_models.py
    ├── test_rounds_service.py
    └── test_api.py
```

---

## 6. 最終検証手順

1. `uv run pytest -v` — 全テストパス
2. `uv run ruff check .` — lint パス
3. `docker compose up --build` → `curl localhost:8000/api/health` が 200
4. ブラウザで: 練習会作成 → 16名（うち初心者2名）登録 → 表示画面で生成・開始・スキップを各数回 →
   途中で1名を休憩・1名を追加 → 管理画面の参加回数一覧で公平性を目視確認
5. 2つのブラウザ（別端末想定）で管理画面と表示画面を同時に開く。管理画面でメンバーを休憩にしても
   タブレット側のカードが変わらないこと、「スキップ」で次を生成したときに初めて反映されることを確認
6. 同じメンバー構成で練習会を2つ作り、初回のマッチが異なることを確認（練習会シードの効果）。
   メンバーの登録順を変えて3つ目を作っても、順序に起因する規則性が見えないことを目視確認
7. 回転ボタンで4状態を切替え、リロード後も維持されることを確認
8. `DATABASE_URL` を差し替えても起動できることをコード上で確認
9. **テストの有効性確認（ミューテーション的チェック）** — 主要なスコア項目の重みを意図的に 0 にする、
   初心者ペア回避の判定を外す、休憩クレジットの計算を止める、といった欠陥を一時的に入れて、
   対応するテストが **実際に落ちる** ことを確認し、確認後に元へ戻す。
   落ちないテストは仕様を検証できていないので書き直す
10. Phase 7 のレビュー指摘が解消済みであること

---

## 7. 決定の変更履歴

| 日付 | 変更 | 理由 |
|---|---|---|
| 2026-09-22 | 統計を累積カラムからスナップショット導出に変更 | 二重計上の防止と取り消し操作の単純化 |
| 2026-09-22 | `fairness_slack` を固定値から候補集合数による適応ルールに変更 | 人数に比例させると大人数ほど不公平が拡大する。調整すべきは自由度の方 |
| 2026-09-22 | メンバー変更による pending ラウンドの自動無効化を撤回 | 試合中に表示が勝手に変わるのを避ける。反映は次の生成時のみ |
| 2026-09-22 | 同点処理を浮動小数ジッタから reservoir sampling による一様抽選に変更 | 比較の安定性と、登録順が透けないことの保証 |
| 2026-09-22 | `member_profiles` を追加し、ニックネームをキーに練習会をまたいで属性を再利用 | 毎回の入力を省くため。統計は共有せず属性のみ。重複は last-write-wins で振動を許容し、同名は警告のみで許可 |
| 2026-09-22 | 使用コートは `court_index` の小さい方から詰め、未使用コートを表示側で明示する | 8名未満のとき会場でどのコートを使うか迷わせないため |
| 2026-09-22 | 仕様 6d を追加（初心者を含むペアは性別を無視）| ユーザー指示。「初心者を集める」目的と「男女ペア同士にする」目的の衝突が解消した |
| 2026-09-22 | **適応slackを撤回し、厳密公平を既定に** | 実測で前提が誤りと判明。枠を広げても ばらけは改善せず、公平性だけ悪化していた |
| 2026-09-22 | 男女構成のうち「男子ペア対女子ペア」だけを別枠の準ハード制約に分離 | 以前は優先度6の男女構成が優先度1のばらけを押しのけていた |
| 2026-09-22 | 貪欲法に先読み(ロールアウト)を追加、対戦相手の重みを倍に | 最適化対象はマッチの品質。理論超過 19.9% → 15.3% |
| 2026-09-22 | コートを `courts` テーブルに切り出し、途中で試合用から外せるようにした | ユーザー要望。初心者の育成用に1面を練習コートとして空けるため。外れるメンバーは休憩扱い |
| 2026-09-22 | デプロイ先を Vercel に。ローカルの docker compose も PostgreSQL に統一 | ユーザー要望。SQLite はサーバーレスで使えない。ローカルと本番で DB を揃えて差異による不具合を防ぐ |
| 2026-09-23 | 画面を3つに分けた（管理 / 全体表示 / メンバー用） | ユーザー要望。役割と利用者が違う。リーダーは読み上げ、メンバーは自分のコートだけ見たい |
| 2026-09-23 | 回転機能を廃止し、コート名で会場と対応させる | 全体表示画面をスクロール可・縦積みにしたので不要になった。使われない機能を残さない |
| 2026-09-23 | メンバー用画面への導線を QR コードにした | 各自がスマートフォンで読み取れるように。サーバ側で segno を使い SVG を返す |
| 2026-09-23 | 練習会の識別子を連番からトークン（10文字）に | 他の練習会の URL を推測できないようにするため |
| 2026-09-23 | コートの状態に `waiting` と `next_round` を追加 | 「まだ生成していない」「試合用に戻した直後」を人数不足と取り違えていた |
| 2026-09-23 | 名前を性別・初心者で色分け。初心者の緑は練習会ごとに切り替え | ユーザー要望。緑はアルゴリズムの確認用で、実運用では出さない |
| 2026-09-23 | **履歴の初心者判定を、現在のレベルから当時のレベル（`round_participation.level`）に変更** | 途中でレベルを変えると過去の負担が消えたり遡って計上されたりしていた。実測で履歴が6回分ずれ、受け持ち回数の幅が広がることを確認 |
| 2026-09-23 | 出場中の人を生成後に編集したら全体表示画面に注意を出す（`members.updated_at` で判定） | 表示中のマッチは動かさない方針なので、食い違いは注意書きでしか伝えられない |
| 2026-09-23 | 管理画面のメンバー一覧でレベルを変更できるようにした | API にはあったが画面に無く、「慣れたらレベルを上げる」運用が実行できなかった |
| 2026-09-23 | 同席テストの閾値を「最少14人」から「最少13人＋平均14.5人以上」に変更（**ユーザー承認のうえでの例外**） | `strength_gap` の導入で最少が10シード中2つで13人に下がる。重みを5に下げても同じ悪化が出るので機能そのものの代償。強さを揃えることを優先すると判断した。最少は1人の運で揺れるため、平均の下限を併せて入れて歯止めにした |
| 2026-09-23 | **（事後記録）** `fix(phase3): 組み分けをコート数に依存しない方式に変える` に含めた2件のテスト変更 | 実装コミットに同梱して記録も承認も取っていなかった。監査で指摘されたので事後に記録する。(1) 同席テストのアサーション緩和「全員（15人）必達」→「最少14人」: 全員必達は `doc/spec.md` のどこにも無い要求で（仕様は優先度1「ばらけること」のみ）、どのシードでも成立しない過剰な条件だった。(2) 固め休みテストの構成差し替え 10名2面→9名2面: 10名2面は毎ラウンド2人が余るためローテーションの揺れが休憩ぶんの欠損に重なり、テストが意図を誤って表現していた。同コミットは代わりに「他の8名は3ラウンドとも出場」という前提の検証を追加しており、緩和ではなく分離になっている。なお 10名のケースは `test_a_long_rest_costs_one_match_even_when_slots_are_spare` として復活させた |
| 2026-09-23 | `revision` を手組みの文字列から応答本体の指紋に変更（**ユーザー承認済み**） | コート名・練習会名・待機/休憩の顔ぶれが入っておらず、変えても表示画面に反映されなかった。`doc/spec.md` l.39-40「全体表示画面とメンバー用画面の内容は自動的に同期する」に反する。手で列挙する限り入れ忘れが必ず起きるので、中身から導く形にした。既存テスト2件のアサーションを「revision が変わらない」から「マッチの組み合わせが動かない」に変更（不変則12が守るのは組み合わせであって表示のされ方ではない） |
| 2026-09-23 | 状態遷移を条件付き UPDATE にし、pending の後片付けを adopt に追加 | 別端末の同時操作で、開始したラウンドが rejected になって統計から消える／pending が2本残って開始したマッチが隠れ二重採用できる、という不具合があった |
| 2026-09-23 | 仕様4のテスト閾値を `<= 2` から `<= 1` に強化し、対照テストを追加 | 機構（`beginner_spread`）を殺した実測値がちょうど 2 で、仕様違反を検出できていなかった |
| 2026-09-23 | 出場者を制約の強い順（初心者→ルール未習得→その他）に並べてから組み分けを探す | 探索は「まだ使っていない中で先頭の人」を起点に進むので、初心者が末尾に固まると最後の組で避けようがなくなる。初心者は後から登録されて id が大きくなりやすく、12人3面で24/36ラウンド、16人4面で11/12ラウンドで初心者同士ペア（仕様3a）が出ていた。同じ制約どうしは id 順のまま（変えるとレベル差の無い練習会でばらけが落ちる） |
| 2026-09-23 | 刈り込みの同点を、タプルの辞書順からハッシュに変更。塩は練習会のシードから導く | 同点が member_id の辞書順で決まっており、12人3面・全員同属性で member 1 の同席者が id 2,3 に固定されていた（平均 id 3.15、一様なら 7.0）。修正後 7.00。**乱数で解いてはいけない**: 刈り込みのたびに引くと候補ごとに消費数が変わり、先読みの共通乱数法が崩れてばらけが 0.190→0.223 に悪化した。ハッシュなら引かずに済む |
| 2026-09-23 | ビーム幅 64 → 512 | 優先度5（初心者ペアの集中）のため。12人3面・24ラウンド・10シードで、片側だけ初心者の試合が 2.07→1.73/ラウンド（理想1.0）。仕様3a を守っているのは上の並べ替えであって幅ではない（64 でも 0 件） |
| 2026-09-23 | `avoid_match` を 5,000 → 1,000 | ルール未習得者同士ペア（5,000）と同値で、「スキップ済みの試合を1つ避けるために仕様3c違反を作ってよい」という順序になっていた |
| 2026-09-23 | 優先度の実装が仕様の並びと食い違うことを `doc/spec.md` に明記（**ユーザー承認済み**） | 優先度2はハード制約として全部より上に立ち、その内側では 5 と 3 が 1 より強い。重み付き和は本質的にレキシコ順にならないため、実測で品質が最も良くなる配分を採る |
| 2026-09-23 | tennisbear のイベントから参加者を取り込む（Phase 9） | 練習会のたびに十数人を手入力するのが重い。公開 API が無いのでページの埋め込み状態を読む。レベルは2軸から推定し、外れる前提で管理画面から直す |
| 2026-09-23 | 取り込みで既存メンバーの属性を上書きしない。ニックネームだけ追従する | 管理者が直したレベルを毎回戻すと直す意味が無い。一方で呼び名が変わったまま読み上げると混乱する |
| 2026-09-23 | `member_profiles` を tennisbear の ID でも引けるようにした | ニックネームは識別子ではない（不変則14）ので、改名されると属性を見失う |
| 2026-09-23 | 練習会名の重複を禁止（**ユーザー判断**） | 選択画面が名前だけを出すので同名だと見分けられない。当初は日時の併記を提案したが、作らせない方針に決定 |
| 2026-09-23 | 性別の「その他」を「未設定」に（表示のみ） | 取り込みでは性別が分からないことがあり、「その他」だと選んだように見える |
| 2026-09-23 | 練習会に紐づく tennisbear のイベントを1つに縛る（**ユーザー指示**） | 別のイベントを取り込むと、その一覧に居ない人が一斉に休憩へ回る。IDを打ち間違えたときに黙って起きると事故になる。最初の取り込みで `practice_sessions.tennisbear_event_id` を確定し、以後は別IDを 422 で断る。管理画面の入力欄も read-only にする。API の公開フィールドが1つ増えたので `tests/test_api.py` の集合比較に追随（緩和ではない） |
| 2026-09-23 | 取り込み元の確定を条件付き UPDATE の rowcount で判定（レビュー指摘） | 在メモリの値を見てから書くと、2台から同時に初回の取り込みを押したときに両方通り、あとの側で全員が休憩へ回る。不変則12（別端末からの同時利用）に沿って `_claim` と同じ形にした。`import_participants` の `event_id` はキーワード必須にして、渡し忘れを型で止める |
| 2026-09-23 | 別イベントの判定を、ページ取得の**前**に移した（レビュー指摘） | 後ろに置くと、打ち間違えたIDが実在しないときに「イベントが見つかりません」が先に返り、本当の理由（別のイベントは取り込めない）が伝わらない。無駄な外部リクエストも消える |
| 2026-09-23 | `member_profiles` を `people`（メンバー台帳）に格上げし、練習会から切り離した（**ユーザー指示**） | 練習会の「削除」が DB から人を消しているように見えていた。実際は参加者行を消すだけだが、人の台帳が入力補完用の辞書でしかないのが原因。練習会の削除は参加者リストから外すだけ、台帳の削除は本当に消す（ただし参加者と記録には触らない）に分けた |
| 2026-09-23 | 同名の番号を台帳で振る（**ユーザー承認済み**） | 台帳から選ぶときに「マッツ」が2つ並ぶと選べない。番号はその人のものになるので、練習会をまたいでも変わらない。代わりに、その週に1人しか参加しなくても「マッツ2」と出る。不変則14 の文言と、既存テスト3本（同名の扱い・last-write-wins・取り込み時の番号）を改訂 |
| 2026-09-23 | 取り込みは**ユーザ ID だけで繋がる**（**ユーザー指示**） | 名前・性別・レベルを写すのは初めて台帳に載せるときだけで、以後は一切同期しない。同名で番号が付いた人はたいてい別の呼び名に変えたくなるので、こちらで管理する方が自然。上流の改名に追従するテスト2本を、追従しないことの検証に差し替え・削除 |
| 2026-09-23 | マルチテナントのスキーマを先に確定（**ユーザー判断**） | 試験運用に入るので、後から DB を作り直したくない。`owners`/`admins`/`owner_admins` をいま入れ、一意制約を団体の中に閉じた（練習会名・取り込み元の識別子）。`owner_admins` を中間表にしたのは、1人が複数の団体を見る形もテーブルを変えずに表せるため |
| 2026-09-23 | 不変則8を改訂: 作り直さない（**ユーザー判断**） | 試験運用のデータを捨てられなくなる。テーブルを変えざるを得ないときは、サービスを止めて使い捨ての移行スクリプトを書く |
| 2026-09-23 | Phase 11 の移行スクリプトを `scripts/migrate_phase11.py` に置いた（レビュー指摘） | 同じ PR で「作り直さない」と決めたのに移行手段が無かった。既存 DB に新コードを当てると `no such column: practice_sessions.owner_id` で起動しない。列を足し、`member_profiles` を `people` に移し、参加者を台帳に繋ぐ。古い列は消さずに残す。手順は `README.md`。phase-10 相当の DB で実行して確認済み |
| 2026-09-23 | CI とデプロイを GitHub Actions で三段にした（**ユーザー指示**） | PR は速い一式（lint・テスト・Vercel と同じ形での起動・コンテナ）、main への push でブラウザテストを足して release の前提にし、release を打ったら本番へ。release ワークフローは、そのコミットの CI が green かを先に確かめる |
| 2026-09-23 | ブラウザテストは「主要な径路だけ」を pytest に載せた（**ユーザー判断**） | 一式（約20本）は実時間を待つものが多く CI で不安定になる。合言葉の門・台帳の CRUD・参加者の追加と「外す」・マッチ生成と開始・メンバー画面が合言葉なしで開けること、の4本。実測 17 秒。`browser` マーカーで既定の `pytest` からは外してある |
| 2026-09-23 | 取り込み元を識別子に含める（`bear:1614380`、**ユーザー指示**） | 別のプラットフォームから取り込む日に備える。カラムは分けない。一意キーが2列で済み、プラットフォームが増えてもテーブルを変えずに載る |
| 2026-09-23 | 固定の管理者1人＋固定パスワード（**ユーザー指示**） | 管理者の登録画面は作らない。いたずら防止であって秘密を守る仕組みではないので、練習会のトークンで開く画面には掛けない。`is_bootstrap` の行は起動のたびに `ADMIN_PASSWORD` からハッシュを作り直すので、パスワード変更で DB を作り直さなくてよい |
| 2026-09-23 | 合言葉の範囲を広げ、**メンバー用画面が使う読み取り以外すべて**に掛けた（**ユーザー指示**） | 当初は「トークンを要さない入口だけ」にしていたが、それだと管理画面の名簿欄や全体表示画面のボタンだけが動かない、という半端な状態になる。「トップ画面経由を強制する。メンバ画面にはかけない」との指示で、ルータを2本に分け、公開するのは health / login / logout / `GET /api/sessions/{token}/current` の4本だけにした。`doc/spec.md` と `CLAUDE.md` も追随 |
| 2026-09-23 | 試合時計を追加（Phase 10）。`CLAUDE.md` の「やらないこと」から外した | ユーザー要望。仕様書で後回しにしていたが、実際に使ってみて必要と判断 |
| 2026-09-23 | 時計は締切ではなく経過秒を持つ | 試合の最中に持ち時間を変えても、その場で正しい残り時間になるため |
| 2026-09-23 | 時計を `revision` に含めない | 経過秒は毎回変わるので、含めると2秒ごとに全体表示画面が描き直され、読み上げ中にちらつく |
| 2026-09-23 | メンバー用画面は手元で数え、時間切れはサーバで確かめてから鳴らす | 手元の時計だけで鳴らすと、一時停止されていたのに鳴ってしまう |
| 2026-09-23 | 時間切れの音を止めたことをラウンドの状態として持つ | 端末ごとに持つと、リーダーが止めてもメンバーのスマートフォンで鳴り続ける |
| 2026-09-23 | メンバー用画面に、その端末だけの停止ボタンと鳴動設定を置く | 手元で止めたい人と、そもそも鳴らしたくない人がいる。ほかの端末には影響させない |
| 2026-09-23 | サーバのプロセスを識別する値を返し、再起動を画面で知らせる | 再起動をまたぐと応答が途切れる。黙っていると「時計が狂った」ようにしか見えない |
| 2026-09-23 | 試合中は「次のマッチ」を出さない | 読み上げている途中で組み合わせが変わってしまう |
| 2026-09-23 | 休憩・離脱になった出場者を暗く表示する | 組み合わせは動かさないので、読み上げる前に気づける必要がある |
| 2026-09-23 | メンバー用画面にレベルを匂わせる表示をしない | 初心者の緑と「初」の印は全体表示画面だけ。知らせも「登録情報が更新されました」に留める |
| 2026-09-24 | サーバーレスでも接続を使い回す（`NullPool` → `QueuePool`、**ユーザー承認済み**） | 「関数インスタンスは短命」という前提が実測と違った。温まっている間はくり返し使われ、毎回 TLS から張り直すと遠い DB（Neon はアジアだとシンガポールのみ）では**それだけで2秒**かかっていた。実測 2.3秒 → 0.35〜0.45秒。不変則8の記録義務に基づく記載。`tests/test_db_config.py` の `assert kwargs == {"poolclass": NullPool}`（実装の固定）を、`poolclass is not NullPool` ＋ `pool_size` ＋ `pool_pre_ping` の性質に書き直した（緩和ではない） |
| 2026-09-24 | 定常1本のまま、あふれ分を 3 まで許す（レビュー指摘） | API は全部同期の `def` なので Starlette がスレッドプールで並行に捌く。つまり1インスタンスが同時に複数のリクエストを持つ。上限1本だと終盤に端末が揃って聞きに来たときに `pool_timeout` を超えて 500 になる。あふれた接続は返却時に閉じるので貯め込みにはならない。テスト2本の `max_overflow == 0, "貯め込まない"` を `0 < max_overflow <= 4` に改めた（「貯め込まない」を担うのは `pool_size=1` の側で、元のアサーションは性質を取り違えていた） |
| 2026-09-24 | 起動時の点検を「管理者がいるか」だけにしていたのを、**テーブルの照合**も足した（レビュー指摘） | 1往復に削った結果、モデルにテーブルを足して配ってもそれが永久に作られず、触った瞬間に 500 になっていた（再現確認済み）。不変則8（`create_all` で用意する）はこの点検に支えられている。2往復に増えるが、`create_all` の N 往復よりはるかに安い。`test_the_check_costs_one_round_trip`（1往復を固定）は要求を取り違えていたので、`test_the_check_does_not_scale_with_the_number_of_tables`（テーブル数によらず一定）に書き直し、欠落の検出テストを**追加**した |
| 2026-09-24 | 起動時の用意に失敗してもアプリは立ち上げる（レビュー指摘） | 例外を投げるとアプリ全体が起動せず、静的ファイルもメンバー用画面も含めて全部 500 になる。DB の一時的な不調なら次の起動で整えばよい |
| 2026-09-24 | 合言葉の作り直しは、同じ環境変数につきプロセスごとに一度だけ（レビュー指摘） | `/api/login` は合言葉を要さない入口なので、失敗のたびに作り直しを試すと外から pbkdf2 を20万回**余計に**まわさせられる。認証の迂回にはならないが、無料枠の実行時間を2倍の効率で焼く |
| 2026-09-24 | メンバー用画面の間引きは 15 秒のまま据え置き（レビュー指摘への判断） | 点数で試合が終わるとリーダーは時間切れを待たず「中断」を押すので、終盤の加速が効かない経路がある（最大15秒・平均7.5秒の遅れ）。リーダーが中断してから次を作って確定するまでの手間がそのぶんを覆うため据え置き、代わりに `member.js` のコメントに遅れと実際の削減量（25,920 → 約17,800 回。6,000 回という見積もりは誤り）を明記した。短くしたい場合は `POLL_IDLE_MS` の一行 |
| 2026-09-24 | 全体表示画面は、人が触ったらその場で間隔を計算し直す（レビュー指摘） | 時刻を書き換えるだけでは、すでに仕掛かっている60秒の待ちが満了するまで戻らない。置き忘れから戻ったリーダーが触っても最大1分だけ追従が遅いままだった。`startPolling` に `poke()` を足した |
| 2026-09-24 | 早すぎるペア重複（500）・同じ4人（240/回）・直前のラウンドと同じ3人（120）の減点を追加（**ユーザー指示**。PR #19） | 同じ相手とのペアは当人が必ず気づくが、まだ組んでいない相手がいるのに2回目を組むことが1人あたり約1回あった（ペアの重複1つを避けると対戦の重複を2つ以上抱えることが多いため）。ペアの減点だけだと同じ4人の組み替えに逃げて顔ぶれが固まるので、4人・3人の減点で塞ぐ（見通しの良い形として、ユーザーと合意）。`History` に `group_count` と `last_round_groups` を足し、`build_history` で採用済みの試合から導出する（スキーマ変更なし） |
| 2026-09-24 | 初心者の集中の重み 700 → 1,000（**ユーザー判断**。PR #19） | 初心者が片側だけの試合は経験者が手加減する育成試合。初心者どうしのカードは集められるうちに使い切り、差のある試合を後回しにする（初心者が慣れる時間も稼げる）。700 だと早すぎるペア重複と競合して10名2面・初心者2で9ラウンド目から出た。差の大きい試合だけに追加減点する案は、特定の人に育成が寄るので不採用 |
| 2026-09-24 | 既存テスト `test_the_beginner_burden_is_uneven_without_the_mechanism` の対照条件を変更（**ユーザー承認済み**。PR #19。レビュー指摘で手続き漏れに気づき、事後に承認を得た） | 仕様の優先度4（初心者と組む回数を非初心者間で均一）の対照テスト。変更前 `dataclasses.replace(Weights(), beginner_spread=0)`、変更後 `dataclasses.replace(Weights(), beginner_spread=0, premature_repeat=0, same_group=0, recent_trio=0)`。アサーション `max(counts) - min(counts) > 1` は変えていない。新しいばらけの減点も初心者と組む相手を散らす方向に働き、`beginner_spread` だけを切っても受け持ちが均されて対照が成り立たなくなったため（テストが「均す機構が無ければばらつく」という性質を表せなくなっていた）。本体のテスト `test_partnering_a_beginner_is_shared_evenly`（幅 ≤ 1）は変えていない |
| 2026-09-24 | 早すぎるペア重複は 500 のまま、連続不参加の1段ぶん（360）より強くしておく（**ユーザー判断**。レビュー指摘） | `doc/spec.md` に「優先度5と3より弱い」と誤って書いていたので、実装どおりに訂正した。350 と比べると、連続不参加が2回続く回数は減る（13名2面で17→11）が、2面を中心にペアの早すぎる重複が1.4〜1.7倍に増える（13名2面で0.27→0.47/人）。当人が気づきやすいペアの重複を優先する。連続不参加の最大はどの構成でも2以下 |
| 2026-09-24 | 古いデプロイを消すスクリプトの残し方を、「環境ごとに最新の READY を1つ」から「外向けの URL（エイリアス）が指しているもの」に変えた（**ユーザー指示**） | 最新で選ぶと、ロールバック直後のように URL が古い方を指しているとき、使われているデプロイを消してしまう。レビュー指摘を受けて、消してよい状態（READY / ERROR / CANCELED）を明示し、環境ごとの最新の READY と作成10分以内のものも残し、消す直前にもエイリアスを取り直すようにした（`STAGING_DOMAIN` 未設定の構成や、一覧の取りこぼしでも使われているものを消さないため）。エイリアスが取れなければ何もしない。判定を通信から切り離し、`tests/test_prune_deployments.py` で確かめる。実データ（`vercel api` で取得）でも判定を確認した |
| 2026-09-24 | バージョンを 0.2.0 に上げ、選択画面の一番下に表示する（**ユーザー指示**） | PR #19（ばらけの改善）と #20（高速化）を含む。バージョンは `app/__init__.py` の `__version__` に持つ（Vercel の関数には `app/**` しか載らず、`pyproject.toml` やパッケージのメタデータを実行時に読めないため）。`pyproject.toml` との一致はテストで照合する。取得は合言葉の要る `GET /api/version`（**ユーザー判断**）。health の応答は CI とリリースの点検が完全一致で照合しているので足さず、公開する入口も増やさない。代わりに合言葉を通る前は表示されない |
| 2026-09-24 | release の事前確認にタグと `app.__version__` の一致を、CI に `uv lock --check` を足した（レビュー指摘） | バージョンを上げ忘れたままタグを打つと、選択画面に古いバージョンが出たまま本番へ出る。`uv sync --frozen` はロックが `pyproject.toml` と食い違っていても黙って入れるので、上げ忘れを CI で止める |
| 2026-09-24 | CLAUDE.md に「対戦カードの考え方」「作業の進め方」を足し、テストの規則を強めた（**ユーザー指示**。PR #23） | 開発は作業ブランチで行い、動作確認ができたら開発要素ごとに main から切った feature ブランチへ積み直して PR に分ける。PR ごとにレビューエージェントが設計レビューとコードレビューをし、CI は PR ごとに green にする。PR を作るのは Claude、取り込むのはユーザー（取り込み前にユーザーがレビューする）。main の取り込みは rebase で、feature ブランチへの `--force-with-lease` は確認なしでよい（`--force` と main への強制 push はしない）。バグは落ちるテストを先に書いてから直す。既存テストがおかしいと考えたら変える前に止まって説明し、承認を待つ。返答の最後に「テストファイルを変更したか」を書く |
| 2026-09-24 | CLAUDE.md に「ドキュメントと計測」「性能の予算」「外部サービス・秘密情報・リリース」「画面を変えたとき」を足した（**ユーザー判断**。PR #23） | 確かめたことだけを書く、実測値は再現できるようにする、性能は 2026-09 の実測を基準にし2割を超えて遅くなるなら諮る、外部サービスへの書き込みは先に読み取りで確かめる、秘密情報を書かない、画面を変えたら実際のブラウザで確かめる、`doc/spec.md` はユーザーの指示があるときだけ変える。**リリースはユーザーが行い**、Claude は頼まれたらリリースの健全性を確かめる。バージョンは振る舞いが変わればマイナー、修正だけならパッチ。あわせて「やらないこと」の古い記述（3面以上のコート UI。すでに4面まで対応済み）を消した |
| 2026-09-25 | 休憩のみなし出場を「長さ − 1」から「休んでいる間の他の人の出場の見込み − 1」に変え、端数のまま計算して生成は整数部分で出場者を選ぶ（**ユーザー判断**） | ユーザーが示した3原則（長い休憩から戻った人は試合に戻す／短く抜けた人は損も得もしない／遅刻した人は参加時点からの公平さ。doc/spec.md 冒頭と一致）で確かめると、原則1が出番待ちの多い構成で破れていた（16名2面で6ラウンド休むと、戻ってから4ラウンド目まで出番が無い）。「長さ − 1」は全員が毎ラウンド出る前提の計算だった。見込みは各ラウンドの出場の割合の合計で、記録から導出する（保存するものは増えない。スキーマ変更なし）。丸めると欠損が1試合分を超えて開くので端数のまま持ち、生成の枠と評価関数は整数部分（スコアは整数のまま）。休憩の無い練習会と全員が毎回出る構成での休憩は、出力が main と完全に一致することを確かめた |
| 2026-09-25 | 既存テスト `test_a_long_rest_costs_one_match_even_when_slots_are_spare` を変更（**ユーザー承認済み**） | 仕様条項: doc/spec.md 冒頭「2試合分以上固めて休んだ場合でも、1試合分の不参加、という扱いでよい」。テストはみなし出場の計算式（長さ − 1）そのものを固定していた。変更前 `assert rested.rest_credit == 2` と `assert rested.adjusted == before + 2`、変更後 `assert rested.adjusted == before + gained - 1`（`gained` は休んでいる間の他の人の adjusted の伸びの平均）。10名2面では休んでいる間の出場の割合が 8/9 で、旧式だと欠損が約0.67試合分しかなく、人数によって欠損の大きさが変わっていた。厳密な一致（`==`）は保ち、他のアサーションは変えていない |
| 2026-09-25 | 離脱から戻った人を、戻った時点の最小 adjusted から再開させる（**ユーザー判断**） | 同じ行を復帰させ、下駄を最小 adjusted に置き直していたが、離脱前の出場回数が残るので adjusted が「最小値 + 離脱前の出場回数」になり、その回数と同じくらいのラウンド数だけ出番が回らなかった。下駄から出場回数とみなし出場（整数部分）を差し引く。離脱前の休憩で積み上がった不足は帳消し（途中参加と同じ扱い） |
| 2026-09-25 | 生成の出場の枠で使う整数化を、切り捨てから四捨五入に変えた（**ユーザー判断**。PR #24 の設計レビュー指摘） | 切り捨てだと、休憩から戻った人がみなし出場の端数の分だけ決まって余計に取り戻していた（16名2面・5ラウンド休憩で平均 +1.13 試合、12名1面・5ラウンドで +0.82）。四捨五入ならこの偏りはほぼ消え（それぞれ −0.29、−0.27）、戻った直後に出場することは全場面で保たれた。離脱から戻った人の下駄も同じ四捨五入で揃える。`PlayerStat.adjusted_whole` は実態に合わせて `adjusted_rounded` に改名 |
| 2026-09-25 | 所要時間の計測スクリプトで、1秒超の判定を平均で行い、1生成ごとの最大は参考として表に出す（**ユーザー判断**） | `doc/algorithm.md` の「所要時間」の表が平均なので、判定もそれに揃える。最大は1回の揺れで1秒を超えうる（20名4面）ので、印には使わず見えるようにだけする。既定の構成を CLAUDE.md の「評価する構成」（12・13・16名の2面と3面）に揃え、回ごとに先に測る側を入れ替え（順番の偏りを減らすため）、相手に `app/` が無いとインストール済みの自分側を黙って測ってしまうのを止めた |
| 2026-09-25 | 既存テスト `tests/test_bench_generate.py` の4件を、計測結果の形の変更に合わせて変更（**ユーザー承認済み**） | 仕様条項ではなく、スクリプトの戻り値が「構成 → 平均」から「構成 → {平均, 最大}」に、印が真偽の `flag` から理由の並び `reasons` に変わったため。(1) `test_a_small_configuration_can_be_measured`: 変更前 `assert result["8名2面"] > 0`、変更後 `assert result["8名2面"]["mean"] > 0`。(2) `test_a_ratio_above_the_limit_is_flagged`: `assert row["flag"] is True` → `assert row["reasons"] == ["比 1.2 超"]`。(3) `test_a_ratio_within_the_limit_is_not_flagged`: `assert row["flag"] is False` → `assert row["reasons"] == []`。(4) `test_over_a_second_is_flagged_even_without_a_comparison` を `test_over_a_second_on_average_is_flagged_even_without_a_comparison` に改名し、`assert row["flag"] is True` → `assert row["reasons"] == ["平均1秒超"]`。入力の数値と他のアサーションは変えていない。(2)〜(4) は印の有無だけでなく理由まで完全一致で見るので、弱めてはいない |
| 2026-09-25 | 行長を 100 → 120 にし、全体に `ruff format` を当て、CI で `ruff format --check .` を確かめる。コミットの前に必ず整形する（**ユーザー指示**。PR #29） | lint しか通しておらず、整形のずれが積み上がっていた（100桁の設定なのに88桁前後で手で折り返していた）。整形の前後で構文木が一致することを確かめた |
| 2026-09-25 | 各試合に「第n試合」の番号を付け、全体表示画面ではコート名の前に、メンバー用画面では試合の上に出す（**ユーザー指示**。数え方はラウンドごとではなく1試合ごとの通し番号: **ユーザー判断**） | 読み上げで「第5試合、Aコート」と呼べるようにするため。番号は保存せず、採用済みラウンドの試合数とコートの並び順から導く（スキーマ変更なし）。このため、スキップ・生成し直し・コートの増減が重なっても番号がずれない。「元に戻す」で取り消した回の番号は、次に開始する回が使い直す（`tests/test_api.py` の試合番号のテスト）。数えるのは COUNT 1本で、表示画面のポーリング1回あたりの SQL は main の11本から12本（4面・採用済みで計測） |
| 2026-09-25 | 取り込み後の定型作業（片付け、CI とデプロイの確認、古いデプロイの除去）をサブエージェントに任せ、CLAUDE.md に役割分担を書いた（**ユーザー指示**） | メインのセッションは開発とレビュー対応に集中し、待ち時間の長い確認作業を切り離すため。古いデプロイの除去は取り込みのたびに行ってよい（ユーザー指示）が、消す側が preview だけのときに限る。本番のデプロイが消す側に入るのは release の後でロールバック先が消えるときなので、`DEPLOY.md` の「戻す可能性があるうちは実行しない」に従ってユーザーに確かめる（PR #33 のレビュー指摘）。止まる条件も CLAUDE.md に並べた |
| 2026-09-25 | PR は draft で作り、レビューへの対応が終わってから draft を外す（**ユーザー指示**）。draft の間は CI を走らせず（**ユーザー判断**）、手元で pytest・ruff（画面を変えたならブラウザテストも）を通してから外す。外したあとの CI が green になれば取り込める状態で、落ちたら draft に戻して直す | PR #33 がレビューへの対応を push する前に取り込まれ、対応を #34 に載せ直すことになった。取り込んでよいかを PR の状態で分かるようにする。draft の間の push ごとの CI は、手元の確かめと重なるので回さない（Actions の実行時間を減らす）。CI は `ready_for_review` でも走るようにした |
