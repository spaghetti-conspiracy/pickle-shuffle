# CLAUDE.md — 開発規則

## このリポジトリ

ピックルボール練習会の対戦カード自動生成ウェブアプリ。

- 仕様: `doc/spec.md`
- 開発手順と進捗: `PLAN.md`
- 仕様と実装が食い違ったら **`doc/spec.md` を正** とする。

## コマンド

- セットアップ: `uv sync`（無ければ `python -m venv .venv && .venv/bin/pip install -e ".[dev]"`）
- 開発サーバ: `uv run uvicorn app.main:app --reload --port 8000`
- テスト: `uv run pytest`
- lint / format: `uv run ruff check .` / `uv run ruff format .`
- コンテナ: `docker compose up --build`

## 設計上の不変則

1. **`app/scheduler/` は DB と Web から完全に独立させる**。`sqlalchemy` / `fastapi` を import しない。
   入力は純粋なデータクラス（`PlayerStat`, `History`）、出力は `RoundPlan` のみ。
2. **統計は必ず `round_participation` スナップショットから導出する**。
   メンバー行に参加回数を累積更新しない（二重計上と更新漏れを構造的に防ぐため）。
   **過去の履歴を「今のメンバー行」で解釈し直さない**。レベルは途中で変わるので、
   そのラウンド時点の値（`round_participation.level`）を使う。現在のレベルで
   過去を数えると、負担が消えたり遡って計上されたりする。
3. **不採用ラウンドは統計に一切影響させない**。スナップショットは採用時にのみ書く。
4. **DB 固有機能を使わない**。生 SQL・SQLite 固有関数・DB 依存の upsert は禁止。ORM 定義を正とする。
5. **フロントはビルド不要を維持**。npm 依存・トランスパイラ・フレームワークを導入しない。
6. 環境変数は `app/config.py` の `Settings` に集約。コード中で `os.environ` を直接読まない。
7. 乱数は必ず `random.Random` を引数で注入する。モジュールレベルの `random` を直接呼ばない。
8. **DB を作り直さない。** 試験運用に入ったので、記録は使い捨てではない。
   テーブルを変えざるを得ないときは、サービスを止めて使い捨ての移行スクリプトを
   `scripts/` に置き、手順を `PLAN.md` に記録する。`create_all` は
   **足りないテーブルを作るだけ**で、既存のテーブルには何もしない。
9. **生成結果は入力リストの並び順に依存してはならない**。アルゴリズムの入口で
   `tie_key(seed, member_id)` により順序を正規化する。ソートのタイブレークに id や登録順を使わない。
10. **同点の候補から先頭を採らない**。必ず RNG で一様抽選する（reservoir sampling）。
    浮動小数の微小ジッタでタイブレークしない。スコアは整数で保持する。
11. **練習会の乱数シードは作成時に採番して以後不変**。生成時の乱数は
    `(seed, round_seq, attempt)` から決定的に導出する。`secrets` や時刻を生成のたびに読まない。
12. **メンバーの変更を pending ラウンドに遡及させない**。反映は次の生成時のみ。
    管理画面と表示画面は別端末から同時に使われる前提で、試合中に表示が勝手に変わってはならない。
13. **練習会をまたいで共有してよいのは属性だけ**。`people`（メンバー台帳）は
    性別・レベルなどの属性のみを持ち、**参加回数・ペア履歴・休憩などの統計は絶対に共有しない**。
    統計のスコープは `members.session_id` が決めるという構造を崩さないこと。
    台帳は団体（`owners`）に属し、問い合わせには必ず `owner_id` の条件を入れる。
    `members` は属性の**写し**を持つ。台帳の行が消えても参加者と記録が無傷で残るため。
14. **ニックネームは識別子ではない**。同名の登録を禁止しない。台帳では2人目以降に
    番号を振って見分けられるようにする（「マッツ」「マッツ2」）。番号はその人のもので、
    練習会をまたいでも変わらない。それでも同名が並んだら警告を出すだけにする。
    コード内でメンバーを指すときは常に `member.id`（台帳なら `person.id`）を使い、
    **ニックネームで人を引き当てない**。引き当ては取り込み元の識別子か id で行う。

## 対戦カードの考え方

評価関数の重みや項を変えるとき、何を良しとするかの拠り所。優先度そのものは `doc/spec.md` が正。

- **目指すのは「皆で集まってやっている感」。** 当人が気づく偏りほど強く避ける。
  同じ相手とのペア（必ず気づく）＞ 同じ顔ぶれ（同じ4人、続けて同じ3人）＞ 同じ対戦相手。
  同じ顔ぶれで固まると集まっている感が薄れる。4 の倍数の人数で起きやすい
- **初心者が入る試合は育成試合。** 経験者は手加減してくれるが、楽しいとは限らないので、
  その負担を経験者全員で分かち合える組み方にする。初心者が複数いれば、初心者を含むペア
  どうしで組んで育成試合を減らし、手加減なしの試合を増やす。そのカードはいずれ尽きるので
  先に使い切り、**差のある試合は後回し**にする（初心者が慣れる時間も稼げる）。
  初心者が1人なら差は必ず出るので、それ自体は避ける対象ではない
- **優先度5（初心者の集中）と3（連続不参加）が1（ばらけ）よりやや強いのは妥当。**
- **評価関数は不用意に複雑にしない。** 何が起きるか予想しにくくなるので、項は増やさない。
  足すなら「この顔ぶれで前に試合をしたか」のように、意図が一目で分かり、効果が分かりやすく
  出るものに限る。ある項が別の逃げ道を生んだら、少し弱い減点でその逃げ道を塞ぐ
  （例: ペアの重複 500 ＞ 同じ4人 240 ＞ 直近の同じ3人 120）
- **良し悪しは重みの合計では比べない。** 重みを変えると合計の意味も変わる。当人が気づく事象の
  回数と時期（何回目の出場で起きたか）、重複回数の二乗和の理論超過など、重みに依存しない指標で
  比べる。1つの指標を良くしたら、他の優先度（出場回数の差・連続不参加・初心者同士ペアなど）が
  崩れていないかも必ず見る
- **評価する構成**: 13名（よくある人数）と、12名・16名（4 の倍数で特殊なことが起きやすい）を
  必ず含め、それぞれ2面と3面（16名は4面も）で見る。初心者やラケット経験者が入る構成も含める。
  シードは複数
- **構造的に避けられないものは、そう書いて受け入れる。** 例: 1ラウンドずつ組む方式では、
  一巡の終わり際のペア重複は避けきれない（避けるには練習会全体の日程表が要る）

## 作業の進め方

- やりとり、コミットメッセージ、PR は日本語で書く。

### 開発から取り込みまでの流れ

1. **作業ブランチにとりあえず積んで開発する。** 試行錯誤はここで行う。
2. **意味が明確な動作確認ができたら、開発要素ごとに feature ブランチへ積み直し、PR に分けて
   main に取り込む。** feature ブランチは main から切り、作業ブランチのコミットを積み直す。
3. **この積み直しを想定してコミットを積む。** 1つのコミットには1つの開発要素だけを入れ、
   別の要素の変更を混ぜない。要素ごとのコミットだけを取り出しても動く（テストが通る）順に積む。
   コミットメッセージには、どの要素の何の変更かが分かるように書く。
4. **1つの PR には1つの関心事。** 振る舞いを変えない変更（高速化など）は振る舞いを変える変更と
   分け、元の実装と出力が一致することを確かめるテストを添える。
5. **PR ごとに、レビューエージェントが設計レビューとコードレビューをする。**
   設計レビューは「狙いに対して考え方と作りが妥当か」、コードレビューは「正しく書けているか」を見る。
   指摘は事実を確かめてから対応し、対応した内容（対応しなかったものは理由も）を PR にコメントする。
6. **CI は PR ごとに green にする。** 他の PR が先に入ることを前提にしない。
7. **PR を作るのは Claude、取り込むのはユーザー。** 取り込む前にユーザーがレビューする。
   Claude は PR をマージしない。レビューエージェントの指摘への対応と CI が green になったことを
   報告し、ユーザーのレビューを待つ。

### その他の決まり

- PR のブランチに main を取り込むときは **rebase** する（マージコミットは作らない）。
  **feature ブランチへの強制 push は `--force-with-lease` なら確認なしで行ってよい。**
  `--force`（lease なし）は使わない。main への強制 push は決してしない。
- 仕様や優先度に関わる判断（重みの順序、公開する入口など）は、計測したデータを添えて
  ユーザーに諮る。ユーザーの判断は `PLAN.md` の「決定の変更履歴」に記録する。
- **消す操作は、先に対象を確かめる。** 「新しいか古いか」ではなく「実際に使われているか」
  （例: デプロイなら外向けの URL に紐づいているか）で残すものを決める。

## コーディング規約

- Python: 型ヒント必須、公開関数に docstring（日本語可）、行長 100、ruff 準拠。
- 用語は仕様書に合わせる。
  - `session` = 練習会
  - `round` = 1回の生成単位（全コート分）
  - `match` = 1コート分の試合
  - `pair` = ペア
  - `slot` = 出場枠
- UI 文言は日本語、コード内識別子は英語。
- 例外は `app/errors.py` の独自例外に集約し、API 層で HTTP ステータスへ変換する。

## テスト規則

- **生成アルゴリズムの変更には必ず単体テストを伴う**。個別の出力を固定値で照合するのではなく、
  仕様の性質（公平性・初心者ペア回避・男女優先度・休憩クレジット）を検証する性質ベースのテストを書く。
- 乱数は固定シードで再現性を担保する。
- API は `TestClient` + インメモリ SQLite で検証する。
- 完了報告の前に `uv run pytest` と `uv run ruff check .` を必ず通す。
- **テストを弱めない。** 通すためにテストを変えるのではなく実装を直す（下の「テストを弱体化させない規則」）。

### バグを直す手順

1. **そのバグで落ちるテストを先に書く。** 仕様の性質として書き、再現した入力をテストケースにする。
2. **直す前に、そのテストが落ちることを確かめる。** 落ちなければ、テストがバグを再現できていない。
   テストの方を直してから進む。
3. 実装を直す。
4. **直した後に、そのテストが通ることと、テスト一式が通ることを確かめる。**
5. 完了報告には、直す前に落ちたこと（失敗の内容）と、直した後に通ったことの両方を書く。

### テストが見張れていることを確かめる

- 新しいテストは、**守りたい仕組みを外す・実装をわざと壊すと落ちること**を確かめる
  （例: 重みを 0 にする、並べ替えを抜く）。落ちないテストは何も見張っていない。
  必要なら、仕組みを外すと落ちることを示す**対照テスト**も置く。
- 閾値には根拠を書く（仕組みがあるとき・無いときの実測値など）。実行結果に合わせて決めない。
- 1つのテストで守る性質は1つにし、テスト名と docstring に「何を守るか」と「なぜ」を書く。

## テストを弱体化させない規則（最重要）

**大原則: テストが落ちたら、まず実装を疑う。テストを通すためにテストを変更してはならない。**

テストは `doc/spec.md` の要求をコードに翻訳したものであり、実装の都合で書き換えてよいものではない。

### 禁止事項

以下は「ユーザーの明示的な承認」なしに行ってはならない。承認なく行われた場合はレビューで差し戻す。

1. **アサーションの削除・コメントアウト・緩和**
   （`assert x == 3` → `assert x <= 5`、`assert len(r) == 8` → `assert r` など）
2. **許容誤差・閾値を実行結果に合わせて広げること**
   （例: 参加回数の最大差の許容値を 2 → 4 に変更する。実装が仕様を満たしていないのが原因）
3. **`@pytest.mark.skip` / `xfail` / `pytest.skip()` の追加**、およびテスト関数・ファイルの削除
4. **「通るシード」への差し替え**。特定シードでのみ落ちるなら、それは実装の欠陥である。
   シードを変えるのではなく、落ちたシードをテストケースとして **追加** する。
5. **`try/except` や `if` によるテスト内の条件分岐で失敗を握りつぶすこと**
6. **性質ベースのテストを、その時の出力をコピペした固定値照合に置き換えること**
   （実装のスナップショットを正解として固定すると、仕様違反を検出できなくなる）
7. **検証対象そのものをモックで迂回すること**
   （generator のテストで generator を、統計導出のテストで stats_rules をモックしない）
8. **テストデータを、たまたま通る都合の良いものに差し替えること**
   （初心者2名のケースが落ちるからといって1名に減らす、など）

### 変更してよい場合と、その手続き

- 変更が許されるのは **`doc/spec.md` 側の要求が変わったとき**、および
  **テスト自体が仕様を誤って表現していたとき** の 2 つだけ。
- その場合は、
  1. どの仕様条項に基づく変更かを `PLAN.md` に記録し、
  2. 変更前後のアサーションを差分として明示し、
  3. ユーザーの承認を得てから変更する。
- **実装が難しいという理由は、テストを緩める根拠にならない。** 実装できないならユーザーに相談する。

### 報告義務

- 既存テストに一行でも手を入れたら、**完了報告で必ずその差分と理由を明示する**。黙って直さない。
- 落ちたまま解決できないテストがある場合、隠したり skip したりせず、
  落ちている事実と原因の調査結果をそのまま報告する。
- `pytest` の実行結果は要約せず、失敗があれば失敗件数と対象を正確に伝える。

### カバレッジの後退禁止

- 既存の振る舞いを変えずにテストだけを減らす変更は行わない。
- バグを修正したときは、**そのバグを再現する回帰テストを必ず追加する**（修正だけで終わらせない）。
  手順は「テスト規則」の「バグを直す手順」。

## やらないこと

- 本格的な認証・認可。管理者は環境変数から作る固定の1人で、登録画面は作らない。
  合言葉は**いたずら防止**であって、秘密を守る仕組みではない。
  掛けないのは**メンバー用画面が使う読み取りだけ**で、それ以外はすべて掛ける
  （`app/api.py` の `public_router` と `router`）
- 3面以上のコート UI（データモデルのみ可変対応）
- DB マイグレーション基盤
