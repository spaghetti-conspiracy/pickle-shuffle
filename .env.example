# docker compose が自動で読む設定。使うときは .env にコピーする。

# 全体表示画面の QR コードに埋め込む URL。
# リーダーがサーバと同じ PC のブラウザで開く場合だけ必要。
# タブレットから LAN の IP で開くなら、設定しなくてよい
# （ブラウザが叩いたアドレスがそのまま QR になる）。
# PUBLIC_BASE_URL=http://192.168.1.10:8000
