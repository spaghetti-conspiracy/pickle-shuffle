# **いまの Vercel は `uv.lock` があるとそちらを使い、このファイルを見ない**
# （ビルドログに `Installing required dependencies from uv.lock...` と出る）。
# 残してあるのは、uv を使わない環境（素の pip）でも動くようにするため。
# 本番に要る依存は pyproject.toml の `dependencies` を正とすること。
# ここだけに書いた依存は本番に入らない。
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
segno>=1.6
psycopg[binary]>=3.1  # pyproject の dependencies にも入れてある
